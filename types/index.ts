export interface Chave {
	NUMERO: string,
	DESCRIÇÃO: string,
	RESTRITO: string
}

export interface Porteiro {
	ID: string,
	PORTEIRO: string
}

export interface Funcionario {
	matricula: string,
	nome: string
}